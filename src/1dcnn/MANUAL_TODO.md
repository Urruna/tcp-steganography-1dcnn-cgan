# 需要人工补充的事项

现有数据上的训练、测试和模型接口已完成。

## 请数据队友补充

要一份生成当前 8 个数据文件的脚本，或让其书面回答：

- 是否按 session/连接划分训练、验证、测试？最好提供各窗口的 session_id 和划分清单。
- direction 的 0/1 各代表什么？interval 使用什么单位？
- write_len 是哪一端、哪一种事件的长度？cum_bytes 如何计算，为什么会有负值？
- y 是按整个会话赋值，还是按窗口是否覆盖调制事件赋值？

这些信息不会阻止当前程序运行，但在正式报告中声称无数据泄漏、跨会话有效之前必须补齐。如果确认原划分混入同一会话，应由数据流水线重新按会话划分并重做 scaler，然后重新训练；无法仅凭现有数组可靠地恢复会话归属。

## 请 GAN 队友后续提供

- `gan_test_X.npy`：按同一定义生成的 `[N,4,128]` 特征。
- `gan_test_y.npy`：与样本对应的真实标签。
- 说明特征是原始值还是已经过本包 scaler 处理。
- 说明样本是生成器候选输出，还是代理实际执行后的重新采集数据。

收到后按 `INTERFACE.md` 的命令运行，即可得到检测结果。目前没有 GAN 数据，相关最终实验需在队友完成后再运行。
